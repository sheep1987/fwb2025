// 数据库连接配置
const dbConfig = {
  host: '127.0.0.1',
  port: 3306,
  user: 'root',
  password: 'root',
  database: 'toubiao'
};

// 服务器配置
const serverConfig = {
  port: 85
};

module.exports = {
  dbConfig,
  serverConfig
}; 